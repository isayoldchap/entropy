package com.sjr.entropy.core

import org.scalatest.{FunSpec, Matchers}

/**
 * Created by srichardson on 4/30/16.
 */

class MoveSpec extends FunSpec with Matchers {

  ignore("A move") {
//    it("A move with a source and no destination is a chaos move") {
//      val move = Move(Some(Point(1, 1)))
//      move.isChaosMove shouldBe true
//      move.isOrderMove shouldBe false
//    }
//
//    it("A move with a source and a destination is an order move") {
//      val move = Move(Some(Point(1, 1)), Some(Point(1, 4)))
//      move.isChaosMove shouldBe false
//      move.isOrderMove shouldBe true
//    }
//
//    it("A move with neither a source nor a destination is a pass move") {
//      val move = Move(None, None)
//      move.isChaosMove shouldBe false
//      move.isOrderMove shouldBe false
//      move.isPass shouldBe true
//    }
  }
}
